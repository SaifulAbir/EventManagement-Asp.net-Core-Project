@{
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
}
@if (ViewData["BookingMessage"] != null)
{
    <br />
    <p class="alert alert-success" id="successMessage">@ViewData["BookingMessage"]</p><br />
    <h3><span class="badge badge-danger">Next Process BookEquipment</span></h3>
    <p>
        @Html.ActionLink("Click Here to Book Equipment", "Equipment", "BookEquipment")
    </p>
}