@{
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
}
@if (ViewData["BookingEquipmentMessage"] != null)
{
    <br />
    <p class="alert alert-success" id="successMessage">@ViewData["BookingEquipmentMessage"]</p><br />
    <h3><span class="badge badge-danger">Next Process Book Food</span></h3>
    <p>
        @Html.ActionLink("Click Here to Book Food", "BookFood", "BookFood")
    </p>
}